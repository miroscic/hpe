Intrinsic Param of RGB Cameras from INI
Intrinsic Param of RASPI Camera from INI
Destructor
Revisione codice plugin HPE per parallelizzare quanto possibile e per usare oneMKL invece di BLAS/LAPACK standard
Controllare compatibilità timestamp tra dummy e realtime

Capire questi possibile BUG
skeleton_from_depth_compute
- Saved files: The code does not write per-joint files here, but the body-index map is used by `point_cloud_filter`
and may indirectly cause point-cloud files to be created (see `point_cloud_filter`).

